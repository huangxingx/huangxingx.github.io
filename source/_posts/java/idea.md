### 设置 Spring 项目以 Service 启动

在 `.idea/workspace.xml`中添加

```xml
  <component name="RunDashboard">
    <option name="configurationTypes">
      <set>
        <option value="SpringBootApplicationConfigurationType" />
      </set>
    </option>
  </component>
```

